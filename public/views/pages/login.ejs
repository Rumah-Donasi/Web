<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Login</title>
  <%- include('../partials/head'); %>
</head>

<body class="bg-gradient-to-br from-dark via-secondary to-dull min-h-screen flex items-center justify-center p-4">
  <div class="bg-white shadow-2xl rounded-xl w-full max-w-md px-8 py-10">
    <h2 class="text-3xl font-bold text-center text-dark mb-8">Masuk ke Akun Anda</h2>

    <% if (typeof error !== 'undefined' && error) { %>
      <div class="mb-4 text-sm text-red-600 text-center border border-red-300 rounded-md p-2 bg-red-50">
        <%= error %>
      </div>
    <% } %>

    <form action="/login" method="POST" class="space-y-6">
      <div>
        <label for="username" class="block text-sm font-semibold text-gray-700">Username</label>
        <input type="text" id="username" name="username" required
          class="mt-1 w-full rounded-lg border border-gray-300 p-3 text-sm shadow-sm focus:outline-none focus:ring-2 focus:ring-dark focus:border-dark" />
      </div>

      <div>
        <label for="password" class="block text-sm font-semibold text-gray-700">Password</label>
        <input type="text" id="password" name="password" required
          class="mt-1 w-full rounded-lg border border-gray-300 p-3 text-sm shadow-sm focus:outline-none focus:ring-2 focus:ring-dark focus:border-dark" />
      </div>

      <div>
        <button type="submit"
          class="w-full bg-dark hover:bg-dark transition-colors text-white font-medium py-2.5 px-4 rounded-lg shadow-md">
          Masuk
        </button>
      </div>
    </form>

    <p class="text-center text-sm text-gray-600 mt-6">
      Belum punya akun?
      <a href="/register" class="text-indigo-500 font-medium hover:underline">Daftar di sini</a>
    </p>
    <p class="text-center text-sm text-gray-600 mt-4">
      Daftar sebagai lembaga?
      <a href="/register" class="text-indigo-500 font-medium hover:underline">Daftar di sini</a>
    </p>
  </div>
</body>
</html>
