<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kartu Isu Donasi</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/js/all.min.js"></script>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: '#10b981',
                        secondary: '#34d399',
                        dark: '#065f46',
                        dull: '#a7f3d0'
                    }
                }
            }
        }
    </script>
</head>
<body class="bg-gray-50 p-8">
    <div class="max-w-4xl mx-auto">
        <!-- Issue Card Section -->
        <div class="mt-8">
            <div class="flex justify-between items-center mb-6">
                <h2 class="text-2xl font-bold text-gray-800">Daftar Isu Donasi</h2>
                <button class="bg-primary hover:bg-dark text-white px-4 py-2 rounded-lg font-medium transition-all hover:scale-105 shadow-md text-sm">
                    <i class="fas fa-plus mr-2"></i>Buat Isu Baru
                </button>
            </div>
            
            <!-- Empty State -->
            <div class="bg-white rounded-2xl shadow-lg border border-gray-200 overflow-hidden">
                <div class="p-12 text-center">
                    <!-- Empty Icon -->
                    <div class="w-24 h-24 bg-gray-100 rounded-full flex items-center justify-center mx-auto mb-6">
                        <i class="fas fa-inbox text-4xl text-gray-400"></i>
                    </div>
                    
                    <!-- Empty State Content -->
                    <div class="max-w-md mx-auto">
                        <h3 class="text-xl font-semibold text-gray-900 mb-3">Anda Belum Memiliki Isu Donasi</h3>
                        <p class="text-gray-500 text-sm mb-6 leading-relaxed">
                            Saat ini belum ada isu donasi yang anda buat. Segera menggalang dana untuk membantu orang-orang yang membutuhkan bantuan.
                        </p>
                        
                        <!-- Action Buttons -->
                        <div class="flex flex-col sm:flex-row gap-3 justify-center">
                            <button class="bg-primary hover:bg-dark text-white px-6 py-3 rounded-lg font-medium transition-all hover:scale-105 shadow-md flex items-center justify-center">
                                <i class="fas fa-plus mr-2"></i>Buat Isu Donasi
                            </button>
                        </div>
                    </div>
                    
                    <!-- Decorative Elements -->
                    <div class="mt-8 pt-8 border-t border-gray-100">
                        <div class="flex justify-center space-x-8 text-xs text-gray-400">
                            <div class="flex items-center">
                                <i class="fas fa-heart mr-2 text-red-300"></i>
                                <span>Bantuan Kemanusiaan</span>
                            </div>
                            <div class="flex items-center">
                                <i class="fas fa-graduation-cap mr-2 text-blue-300"></i>
                                <span>Pendidikan</span>
                            </div>
                            <div class="flex items-center">
                                <i class="fas fa-seedling mr-2 text-green-300"></i>
                                <span>Lingkungan</span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>